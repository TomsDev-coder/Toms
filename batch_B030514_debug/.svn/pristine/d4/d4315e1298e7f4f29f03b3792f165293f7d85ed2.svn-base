//////////////////////////////////////////////////////////////////////////
// Project Name : TOMS
// File Name : CompetitorsCountDto.java
// Encoding : UTF-8
// Creation Date : 2020-01-09
//
// Copyright © 2020 JADA. All rights reserved.
//////////////////////////////////////////////////////////////////////////
package jp.co.seiko_sol.dto;

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import lombok.ToString;

/**
 * 自動アサインバッチ処理（ID：B030514）用の手動役割用ユーザDTOクラス.<br>
 * ユーザID、DCP種別を保持する。
 * 
 * @author IIM
 * @version 1.0
 */
@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
@ToString
public class ManualRoleUserDto {

    /** ユーザID */
    Integer userId;

    /** DCP種別（シニアDCO） */
    Boolean isDcoSenior;

    /** DCP種別（DCO） */
    Boolean isDco;

    /** DCP種別（研修DCO） */
    Boolean isDcoTrainee;

    /** DCP種別（管理者BCO） */
    Boolean isBcoAdmin;

    /** DCP種別（BCO） */
    Boolean isBco;

    /** DCP種別（メンター） */
    Boolean isMentor;

    /** DCP種別（SCO） */
    Boolean isSco;

    /** DCP種別（IDCO） */
    Boolean isIdco;
}
